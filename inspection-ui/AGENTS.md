# Crop Inspection UI

- The approved visual source is `C:/Users/aboul/AppData/Local/Temp/codex-clipboard-908dece2-35e5-4ef0-89eb-28b30b95c287.png`.
- Preserve the slim horizontal pulse rail, matte charcoal transparency, compact agronomic columns, rotating Field Guide, and high-density typography.
- Curves always mean real history plus a dashed no-care forecast. Never add decorative random movement.
- Treat numeric hierarchy as operational UI: Growth/Health, condition percentages, and all time values stay bold and readable at every supported resolution. Curves use adaptive real-value scaling, monotone smoothing, a soft solid area/glow, and a dashed forecast; visual emphasis must never invent oscillation.
- Production starts empty and may render only after a Lua `inspection:open` message. Fixtures are restricted to the browser development entry and must be absent from `dist`.
- The Field Guide rotates six concise agronomy lessons locally, prioritizing the current authoritative diagnosis without presenting an action control. Do not add buttons, chevrons, pointer events, or NUI focus.
- Curves use semantic condition tones rather than category colors: risk is orange-red, watch is amber, good is green, and unaffected is neutral gray. The status from the authoritative snapshot is the only source of tone.
- The rail begins after the minimap and ends at the right safe-zone. Validate 1280×720, 1920×1080, and ultrawide layouts.
- Use Barlow Condensed for display copy, Source Sans 3 for body copy, Phosphor for icons, and canonical crop assets from `inventory_images/`.
